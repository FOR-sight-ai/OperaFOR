[project]
name = "mcp-server-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.13",
    "fastmcp>=2.8.1",
    "huggingface-hub[mcp]>=0.33.0",
    "mcp[cli]>=1.9.4",
    "pywebview>=5.4",
    "uvicorn>=0.34.3",
    "pyinstaller",
    "torch==2.2.2",
    "forcolate>=0.1.17",
    "numpy<2"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
